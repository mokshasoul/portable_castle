# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
#Autostart
# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $mod1 Mod1
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: Hack 7

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.
##Floating windows configuration
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
floating_minimum_size 1x1
floating_maximum_size 800x600

#Wrap Focus
force_focus_wrapping yes

# start a terminal
bindsym $mod+Return exec /usr/bin/urxvtc-256color

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# i3-dmenu-desktop --dmenu='rofi -dmenu'
# old one 
# bindsym $mod+d exec --no-startup-id rofi -show run
bindsym $mod+d exec --no-startup-id rofi -show drun
#Emulate Win+E in windows with thunar ranger is unnesesscary
bindsym $mod+e exec --no-startup-id thunar

# change focus
bindsym $mod+h focus left
bindsym $mod+l focus right
bindsym $mod+j focus down
bindsym $mod+k focus up

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation 
bindsym $mod+o split h

# split in vertical orientation
bindsym $mod+v split v


# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+z layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

#Bind variables to workspace names
set $ws1 1:www
set $ws2 2:main
set $ws3 3:chats
set $ws4 4:dev
set $ws5 5:security
set $ws6 6:media
set $ws7 7:games
set $ws8 8:random

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

#move current window to scratchpad
bindsym $mod+Shift+minus move scratchpad

#display scratchpad
bindsym $mod+minus scratchpad show 

#Enable back and forth
bindsym $mod+Tab workspace back_and_forth

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

#Print screen found script on stackoverflow
bindsym $mod+Print exec scrot -e 'mv $f ~/Pictures/Screenshots/ && gimp ~/Pictures/Screenshots/$f'
bindsym Print exec scrot -s -e 'mv $f ~/Pictures/Screenshots/ && feh ~/Pictures/Screenshots/$f'

# resize window (you can also use the mouse for that)
mode "resize" {
# These bindings trigger as soon as you enter the resize mode

# Pressing left will shrink the window’s width.
# Pressing right will grow the window’s width.
# Pressing up will shrink the window’s height.
# Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

# same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

# back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

set $Locker ~/bin/i3lock-wrapper.sh
#Modes for managing pc power-state
mode "(l)ock (s)leep (p)oweroff (r)estart" {
        bindsym s exec $Locker && systemctl suspend;mode "default"
        bindsym p exec systemctl poweroff;mode "default"
        bindsym r exec systemctl reboot;mode "default"
        bindsym l exec $Locker;mode "default"

        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#Different display settings
mode "(c)lone (e)xtend" {
        bindsym c exec xrandr --output LVDS1 --auto --output VGA1 --auto;mode "default"
        bindsym e exec sh ~/bin/setScreen.sh;mode "default"

        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#Bind to modes
bindsym $mod+p mode "(c)lone (e)xtend"
bindsym $mod+r mode "resize"
bindsym $mod+x mode "(l)ock (s)leep (p)oweroff (r)estart"

#FN KEYS + WINDOWS LIKE Workspace/Container moving
bindsym XF86AudioRaiseVolume exec --no-startup-id sh ~/bin/setAudio.sh plus
bindsym XF86AudioLowerVolume exec --no-startup-id sh ~/bin/setAudio.sh minus
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 10
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 10

#Container screen mover
bindsym $mod+Shift+Left move workspace to output left
bindsym $mod+Shift+Right move workspace to output right
bindsym $mod+Right move container to output right
bindsym $mod+Left move container to output left

#Change language
# bindcode Mod1+50 exec --no-startup-id sh ~/bin/languangeSwitch.sh

#FULL SCREEN FIX
popup_during_fullscreen smart

#### Workspace settings ####
#window border
new_window pixel
new_float pixel
# hide_edge_borders smart
# Stop following mouse (cz of trackpad) 
focus_follows_mouse no

# Focus on window when it spawns
focus_on_window_activation smart

# Focus new window
bindsym $mod+u [urgent=latest] focus

# Workspace orientation of containers
workspace_layout tabbed

# Workspace togglin
workspace_auto_back_and_forth no

# Workspace output location 
workspace $ws6 output HDMI3 

#### Container Assignments ####
# Windows to workspace assignment
# WM_CLASS(STRING) = "instance", "class")
assign [class="^keepassxc$"] $ws5
assign [class="^jetbrains-pycharm$"] $ws4
assign [class="^Thunderbird$"] $ws3
assign [class="^Gpa$"] $ws5
assign [class="Chromium"] $ws1
assign [class="mpv"] $ws6
assign [class="AnyDesk"] $ws4
assign [class="Keepassx"] $ws5
assign [title="\w*-\sVIM\b"] $ws2
assign [class="^Skype$"] $ws3
assign [class="^Firefox$"] $ws1
assign [class="^Soffice$"] $ws2
assign [class="^libreoffice$"] $ws2
assign [class="^ViberPC$"] $ws3
assign [class="^URxvt$" title="^ranger$"] $ws2
assign [instance="urxvt" title="^WeeChat"] $ws3
assign [class="YaST2"] $ws2
assign [class="^URxvt$" title="VIM$"] $ws2
assign [instance="^vim$"] $ws2
assign [class="^Tor Browser$"] $ws5
assign [class="^Zathura$"] $ws2
assign [class="^JDownloader 2$"] $ws1
assign [class="^Eclipse$"] $ws4
assign [class="^Wine$" instance="TeamvViewer.exe"] $ws4
assign [class="^Steam$"] $ws7
assign [class="^URxvt$" title="^$USER- tmux$"] $ws2
assign [class="^Chromium-browser$"] $ws1
assign [class="^URxvt$" title="^root - tmux$"] $ws2
assign [class="^Clementine$"] $ws6
assign [class="^VirtualBox$"] $ws4

#Per container prefs
for_window [class="^Tk$"] floating enable
for_window [class="Mate-menu.py"] floating enable
for_window [window_type="dialog"] floating enable
for_window [title="TeamViewer"] floating disable
for_window [class="^Skype$"] floating enable
for_window [class="^Skype$" title=" - Skype$"] floating disable
for_window [class="^Firefox$"] floating enable
for_window [class="^Firefox$" instance="^Places$"] floating disable 
for_window [class="^Firefox$" instance="^Navigator$"] floating disable
for_window [class="Pavucontrol"] floating enable
for_window [class="gimp"] floating enable
for_window [class="gimp" window_role="gimp-image-window"] floating disable

### Arbitary windows found in https://github.com/deterenkelt/dotfiles/blob/master/.i3/config.template
for_window [class="Xdialog"] floating enable
for_window [class="com-sun-javaws-Main"] floating disable
for_window [instance="sun-awt-X11-XDialogPeer"] floating enable
for_window [class="YaST2"] floating enable
for_window [instance="^sun-awt-X11-XFramePeer$"] floating disable
for_window [instance="^sun-awt-X11-XWindowPeer$" title="^JDownloader$"] floating enable
for_window [instance="^sun-awt-X11-XFramePeer$" title="^JDownloader Updater$"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [class="Steam"] floating disable
for_window [title="\w*\s-\sVIM\b"] move to $ws2
for_window [title="^mpsyt$"] move to $ws6
# For all preferences
for_window [title="^xdg-su: /sbin/yast2$"] floating enable
for_window [title="Preferences$"] floating enable
for_window [class="^spacefm$"] floating enable
for_window [class="^Thunar$"] floating enable
for_window [class="^.*"] border pixel 0


#set screens before startup so tray is fixed
# exec --no-startup-id ~/bin/setScreen.sh
# Disable Plasma
exec --no-startup-id wmctrl -c Plasma
for_window [title="Desktop — Plasma"] kill; floating enable; border none
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
 bar{
          font pango: Ubuntu 7
          status_command i3pystatus -c ~/.config/i3/pystatus.py
          tray_output primary
          strip_workspace_numbers yes
  }

#STARTUP PROGRAMS
# exec --no-startup-id dbus-launch nm-applet  <-- GENTOO
# exec --no-startup-id unclutter
# exec --no-startup-id pasystray
# exec --no-startup-id dropbox start
# exec --no-startup-id /usr/lib/polkit-gnome-authentication-agent-1
# exec --no-startup-id gsynaptics-init --sm-disable
# exec --no-startup-id compton -b --config ~/.config/compton/compton.conf
# exec --no-startup-id parcellite
# exec --no-startup-id dunst
# exec --no-startup-id nm-applet
# exec --no-startup-id udiskie -2 -t
# exec --no-startup-id redshift
# exec --no-startup-id urxvtc-256color -name vim -e vim ~/Documents/todo.txt
# exec --no-startup-id urxvtc-256color -name ranger -e ranger 
# exec --no-startup-id mpd
# exec --no-startup-id ~/bin/setLayoutWorksapce3.sh
# exec --no-startup-id xfsettingsd
# exec --no-startup-id xfce4-power-manager
#exec --no-startup-id  syndaemon -i 1 -t -K -R 
# exec --no-startup-id xfce4-volumed
# exec --no-startup-id xfce4-panel --disable-wm-check
for_window [class="plasmashell"] floating enable
for_window [class="Plasma"] floating enable, border none
for_window [title="plasma-desktop"] floating enable, border none
for_window [title="win7"] floating enable, border none
for_window [class="krunner"] floating enable, border none
for_window [class="Kmix"] floating enable, border none
for_window [class="Klipper"] floating enable, border none
for_window [class="Plasmoidviewer"] floating enable, border none
for_window [class="yakuake"] floating enable, border none
